#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;

    double total_score = 87 * x + 85 * y;
    double total_student = x + y;
    double average_score = total_score / total_student;

    printf("%.4f", average_score);

    return 0;
}

