#include <bits/stdc++.h>

using namespace std;

int main()
{
    double r, V;
    cin >> r;
    V = 4.0 / 3.0 * 3.14 * pow(r, 3);  // pow(x, y)函数可以求x的y次方
    cout <<fixed<<setprecision(2)<< V << endl;
    return 0;
}

